PASOS PARA MONTAR LA MISTER MODELO RETROCASTLE
==============================================

--Libera la placa IO + chapa lateral con microswitches. NO hace falta separarlas para nada.
--Aprieta los pilotes metálicos (los de arriba y los de abajo) que vienen en la placa IO. NO los separes, déjalos puestos y listos.
--Saca la DE10 y quítale TODOS los pilotes metálicos: deja la placa sola.
--Coge la placa I/O y ponle desde la parte de abajo la guía cuadrada para el bloque cúbico de disipación. Va con un par de tornillos pequeños.
--Ensambla la DE10 a la IO.
--Dale la vuelta: trabaja con la gravedad, no contra ella. Ve poniendo las arandelas de plástico, coloca la chapa inferior y atorníllala.
--Pon las chapas laterales (OJO!!! La delantera que es la del puerto Ethernet se puede resistir un poco y no entrar bien en su posición.
  Para solucionarlo, empuja hacia abajo el puerto USB OTG hasta que entre por su agujero y la chapa encaje),
  y por último la chapa superior y atorníllala.

PREPARAR LA TARJETA SD
======================


	****OPCIÓN RECOMENDADA A DÍA DE HOY: USANDO Mr. FUSION****

----Nos bajamos la última release estable de https://github.com/MiSTer-devel/mr-fusion/releases
	Grabamos la imágen en la microSD con DD, arrancamos la MiSTer con ella, y seguimos las instrucciones en pantalla.

----Ahora vamos a configurar la conexión WIFI. Editamos /media/fat/linux/wpa_supplicant.conf lo dejamos así:

####FICHERO WPA_SUPPLICANT.CONF####

ctrl_interface=/run/wpa_supplicant
update_config=1
country=ES

network={
        ssid="tu_ssid_aqui"
        psk="tu_clave_aqui"
		priority=2
}

network={
        ssid="tu_ssid_aqui"
        psk="tu_clave_aqui"
		priority=1
}

####FIN FICHERO WPA_SUPPLICANT.CONF####

Fíjate que podemos tener configuradas varias redes, y se conectará preferentemente a la que tenga un valor "priority" mayor,
si no la encuentra se conectará a la siguiente, etc.

Ahora reiniciamos y ya nos tendría que salir el icono de WiFi como conectado en el menú principal.

----Ahora ya metemos el Downloader.ini con los sistemas que queremos que no se actualicen, el MiSTer.ini que queramos,
e instalamos el Update_All, para lo cual descargamos este zip:
https://github.com/theypsilon/Update_All_MiSTer/releases/latest/download/update_all.zip 
y metemos el script "Update_All.sh" en /media/fat/Scripts.
Ejecutamos el "Update_All" y ya como siempre.

----Ahora activamos el fast polling de los mandos USB. Para ello, simplemente ejecutamos "fast_USB_polling_on.sh".

	****OPCIÓN NO RECOMENDADA, PERO POR SI TE HACE FALTA PREPARAR LA TARJETA A MANO****

No hace falta ningún programa de Windows. Simplemente hazle este layout con GPARTED.
(OJO!!! Crea las particiones como tipo EXT4, da igual, luego lo vas a cambiar con FDISK. GPARTED NO SOPORTA EXFAT.):

New layout is pretty simple:
Part1: Starts from 1MB till the end of SD card minus 1MB at the end. Type: exFAT (code 7)
Part2: Starts from last 1MB of SD card with size of 1MB. Type: Unknown (A2, no existe pero es lo que hay que poner) 
And now dd u-boot binary to Part2.

Usando FDISK, pon el tipo de partición 7 para la primera partición (exFAT) y el tipo A2 para la segunda. Te dirá que el tipo 2A es desconocido, da igual porque la vas a machacar con el u-boot.img. 
El u-boot.img viene de:
https://github.com/MiSTer-devel/SD-Installer-Win64_MiSTer

Formatea la partición 1 como exFAT con: mkfs.exfat /dev/sdb1
(mkfs.exfat está en el paquete exfat-utils)

Y ahora simplemente te bajas los ficheros que han de ir en la SD, que están en la guía:
https://github.com/MiSTer-devel/Main_MiSTer/wiki/Setup-Guide
Y los metes en la tarjeta. Tienes que tener tres cosas principalmente: el directorio linux, y los archivos MiSTer y menu.rfb; Con eso ya arranca desde la SD porque le has flasheado el uboot antes. 
A mayores, pues los cores que le quieras ir metiendo, cada uno tiene sus particularidades pero van el el raiz los rbf de cada core.

MEJORAR LA LATENCIA DE LOS USB
=============================

Crea el fichero
/media/fat/linux/u-boot.txt

Con el contenido
v=loglevel=4 usbhid.jspoll=1 xpad.cpoll=1

TECLADO EN CASTELLANO EN LA CONSOLA DE LINUX
============================================

Añade al final de /etc/profile:
loadkeys es

ROM JIFFYDOS PARA EL C64
========================

from a unix shell I did:

cat 901226-01.bin JiffyDOS_C64.bin JiffyDOS_C1541.bin > boot.rom

Where 901226-01.bin is the C64 BASIC ROM.

Then placed boot.com in my C64 directory on the micro SD card. The C64 core is called "C64".

CONECTARSE A UNA BBS DESDE EL CORE DE C64
=========================================

La información procede de este documento:
https://mister-devel.github.io/MkDocs_MiSTer/advanced/computer/#serial-connection-on-c64
...Al que llegué desde este hilo:
https://misterfpga.org/viewtopic.php?t=6106

--LO PRIMERO: HAS DE USAR EL KERNAL ORIGINAL DE COMMODORE, porque ni el DolphinDOS ni el JiffyDOS tienen las funcionalidades necesarias.

--Nos bajamos el CCGMS en PRG, que lo tienes en: https://csdb.dk/release/?id=173065
ALTERNATIVAS A CCGMS:
Novaterm 9.6c de: https://commodore.software/downloads/download/19-novaterm/653-novaterm-9-6c
O bien el StrikeTerm2014 de: https://csdb.dk/release/?id=130807

--En el core:
---Vamos a "UART mode (Modem)", ponemos "Connection" a "Modem", dejamos "Link" a "TCP" y "Baud" a "9600" .
---Vamos a "Hardware", ponemos "Expansion" a "RS232", "RS232 Connection" a "Internal", "RS232 mode" a "UP9600" y salvamos.

--Entramos en el core y cargamos el PRG del CCGMS y allí:
----Pulsamos F7 para entrar en el menú de opciones de dialing, pulsamos "M" para poner el tipo de módem a "UP9600/EZ232",
    pulsamos "B" para poner los baudios a 9600, y le damos a ENTER para volver al menú principal.
----Escribimos "at" y le damos a intro, nos tiene que decir "ok".
----Ahora marcamos la dirección de la BBS con:
  "atdtstreetcorner.ddns.net:6400"
  (Tienes cuenta en StreetCornet: El login handle es "sc48" y el pass es "brightblade")
ALTERNAVTIVAS A CCGMS:
Cargamos el Novaterm o el Striketerm, y en el menú principal (F1) nos aseguramos de estar a 9600 baudios.
Entramos en el phonebook (F5) y allí añadimos las BBS que queramos, hay unas cuantas aquí: http://cbbsoutpost.servebbs.com/

CARGAR RÁPIDO CON JIFFYDOS Y SIN JIFFYDOS
========================================

Con JiffyDos:
Para listar, F1 e intro.
PARA CARGAR Y EJECUTAR: F5, F1 e intro.

Sin JiffyDOS, 
cargamos con: LOAD"*",8
y luego RUN

Juegos TRANSWARP, como el California Games de: https://www.lemon64.com/forum/viewtopic.php?t=83077
cargamos con: LOAD"*",8,1
y luego RUN

CARGAR JUEGOS EN TRD EN EL CORE DE ZX SPECTRUM
==============================================

Lo primero, como debes saber el core tiene la ROM del Spectrum en /media/fat/spectrum.rom
Vale, pues usa el spectrum.rom que viene con el REAL COMMANDER.
Ahora, simplemente inicia el core, inserta el TRD, pulsa F11, y en el menú elige el IANNA.BAS o como se llame. Listo.

Para resetear, ALT+F11.

Hay juegos que son autoarrancables, como el Fire&Ice. Para estos podrías hacer lo mismo, o bien lanzarlos a mano: 
-entrar en el TR-DOS (esto sale en el menú cargador del Spectrum si estás en modo 128K/+2 o Pentagon)
-ahí dale a la K para LIST si quieres ver los archivos, verás que hay uno llamado BOOT
-ahora dale a la R para RUN, e intro, y el juego irá.
Para cargar el Sword of Ianna, tienes que poner: LOAD "S.IANNA+"
RECUERDA que para escribir LOAD es con la J.

PARA QUE SE VEA TODO  POR HDMI EN TV ANTIGUA SONY BRAVIA
========================================================

El truco es especificar tanto el modo para contenido NTSC como para contenido PAL,
en este caso elijo los modos de 720p a 50 y 60Hz respectivamente.

video_mode_ntsc=0
video_mode_pal=7

PROGRAMA PARA CONVERTIR MODELINES DE XORG A video_mode DE LA MISTER
===================================================================

Simplemente es irle metiendo, en el órden en que salen en la página, los parámetros del modeline que quieras, y que puedes generar con el
generador de modelines de https://arachnoid.com/modelines/
NO copies los decimales del último parámetro del resultado (deberían ser ceros, de todos modos, pero no los copies).

#include <stdio.h>

int main () {
        float clock;
        int hdisp, hsyncstart, hsyncend, htotal, vdisp, vsyncstart, vsyncend, vtotal;

        printf ("CLOCK: ");
        scanf ("%f", &clock);
        printf ("HDISP: ");
        scanf ("%d", &hdisp);
        printf ("HSYNCSTART: ");
        scanf ("%d", &hsyncstart);
        printf ("HSYNCEND: ");
        scanf ("%d", &hsyncend);
        printf ("HTOTAL: ");
        scanf ("%d", &htotal);
        printf ("VDISP: ");
        scanf ("%d", &vdisp);
        printf ("VSYNCSTART: ");
        scanf ("%d", &vsyncstart);
        printf ("VSYNCEND: ");
        scanf ("%d", &vsyncend);
        printf ("VTOTAL: ");
        scanf ("%d", &vtotal);
    
        printf ("video_mode=%d,%d,%d,%d,%d,%d,%d,%d,%f\n", hdisp, hsyncstart - hdisp, hsyncend - hsyncstart, htotal - hsyncend,
                                                        vdisp, vsyncstart - vdisp, vsyncend - vsyncstart, vtotal - vsyncend,
                                                        clock * 1000);

*********APUNTES CORE PCXT***************************************************************

En PCXT BIOS va "pcxt_original.rom", en TANDY BIOS va "tandy.rom", y en EC00 BIOS va "ide_xtl.rom" 

NOTAS SOBRE LAS IMÁGENES DE DISCO PARA EL CORE PCXT
===================================================

La mejor BIOS para XT es la de Juko Electronics, ya que tiene scroll suave en el texto.
Para obtener BIOS ya preparadas tanto de XT como de TANDY 1000, tienes que bajar los scripts de Python de aquí:
https://github.com/spark2k06/PCXT_MiSTer/tree/main/SW/ROMs
...Y luego ejecutas "python <nombre_script>".
La BIOS resultante está lista para usarse. NO tienes que descargar nada a mano, lo hace todo el script.
Si no te arranca de disco duro, carga la BIOS de XTIDE en la opción "BIOS/EC00 BIOS:" del core.

Para arrancar en modo TANDY y con disco duro IDE real, necesitamos una BIOS TANDY limpia (=sin XTIDE incluido, es decir, una BIOS TANDY 1000
que te bajes de aquí: http://retrograde.inf.ua/tandyroms.htm) y cargar la BIOS de XTIDE en la opción "BIOS/EC00 BIOS:" del core.

Si queremos arrancar con una BIOS de XT limpia, tenemos que cargar la BIOS de XTIDE en la opción "BIOS/EC00 BIOS:" del core.
(Si queremos hacer que una BIOS de XT esté "limpia" tenemos que editarla con HEXEDIT y machacar el primer byte,
que es una letra "U", para que no cargue la XTIDE interna que lleva esa BIOS)

Para crear la imágen simplemente desde el propio GNU/Linux de la Mister haz:
dd if=/dev/zero of=hdd.vhd bs=128M count=1

Ahora tenemos que particionarla y formatearla desde la MiSTER, para lo cual tenemos que arrancar desde una imágen de disco de MSDOS.
Lo mejor con diferencia es instalar MS-DOS 5.0 o 3.3 (aunque si instalas 3.3 estás limtado a discos duros de 32MB)
desde imágenes de disco de 320KB 5-1/4 que hay en WinWordPC disponibles para bajar.
-NO uses las imágenes de 720KB porque fallan.
-Si quisiéramos instalar desde una imágen de disco de 1.44MB (no hace falta complicarse con esto...),
para que el core lea imágenes de 1.44MB tenemos que usar la BIOS micro8088, PERO esa viene con el XTIDE integrado,
así que tenemos que editarla con HEXEDIT y machacar el primer byte (una letra "U") para que no cargue la XTIDE interna que lleva esa BIOS.

Una vez que hemos arrancado desde el diskette de MSDOS y el core nos ha reconocido el disco, podemos particionarlo con fdisk y formatearlo
con format.

Para transferir directorios entre discos, montar los discos desde Linux así:
sudo mount -t vfat -o loop,offset=32256 hdd.vhd mnt
(TODOS los discos duros de MSDOS se montan igual)

Para que el primer DIR no tarde taaaaaanto, puedes descargarte el FREESPT.COM de aquí:
https://github.com/ChartreuseK/FREESP/releases
Y meterlo en el AUTOEXEC.BAT como:
LH FREESPT.COM C

Para activar/desactivar los colores COMPOSITE dinámicamente (ya que los juegos en modo PCXT se ven genial con colores composite
pero los colores composite hacen que la consola de texto de DOS se vea FATA) usa el XTCTL.EXE, que está aquí para descargar:
https://github.com/MiSTer-devel/PCXT_MiSTer/tree/main/hdd

Hay muchos juegos preparados para XT de verdad aquí: https://www.pcjs.org
Se pueden descargar dándole a SAVE.

El driver de ratón que pone en los docs del core que hay que usar (CTMOUSE versión 1.9,
que está en https://github.com/spark2k06/PCXT_MiSTer/tree/main/hdd/) no es compatible con el EDIT.COM de MS-DOS, se cuelga el EDIT.
Si el que debes usar es este CTMOUSE.EXE: https://github.com/davidebreso/ctmouse
La diferencia es que este está adaptado a procesadores 8088, ya que el CUTEMOUSE normal usa instrucciones de 286 y por eso falla.
Con este driver funciona tanto el EDIT como los juegos, tanto en modo XT como en modo TANDY.
No necesitas poner "LH" delante cuando cargas el CUTEMOUSE desde el AUTOEXEC.BAT: el driver se carga él solo en memoria alta.
NO USES el driver de Microsoft, ya que sólo funciona en modo Tandy: en modo XT el cursor no se mueve.

NOTAS SOBRE JUEGOS ESPECÍFICOS DE PCXT
--El Might & Magic 2 lo arreglé para que funcionase en TANDY y lo subí a Archive.org y a mi disco de backup.
--El Pool of Radiance de la colección EXODOS funciona en TANDY, pero tienes que editar el archivo POOL.CFG para especificar
la ruta donde lo vas a meter (viene como C:\POOLRAD, así que si lo metes en C:\JUEGOS\POOLRAD no funcionará).
Además, en POOL.CFG puedes poner las dos primeras letras como T, para tener gráficos TANDY y sonido TANDY.
Y además, si le pasas el parámetro STING, no te sale la protección (START.EXE STING).

IMAǴEN DE DISCO MAESTRA PARA ATARIST
=====================================

Viene de este hilo de Retrowiki:
http://retrowiki.es/viewtopic.php?f=108&t=200035098&start=48

Configuración del sistema que hay que usar con ella: ATARI MegaSTE 4Mb. TOS 2.06

CREAR IMÁGENES DE DISCO DURO DE ATARIST Y LUEGO MONTARLAS EN LINUX
==================================================================

PREVIO: El sistema y driver de discos original de Atari para los AtariST se llamaba AHDI. Tiene limitaciones del copón como que sólo puede arrancar desde particiones de tipo GEM de tamaño menor o igual a 16MB,
luego tampoco se pueden hacer particiones GEM de más de 32MB, etc.. es una mierda.
TOTAL, que nosotros usamos HDDRIVER de: https://www.hddriver.net/en/
ES DE PAGO, el fichero se llama hddr1016.st (así como un hddr1016.zip con los contenidos que no deberías tener que usar a no ser
que tengas que reconstruir el .st) y lo tienes en el disco duro de backup y en el correo.
(Si tuvieras que reconstruir el .st, crea un DSK con el HATARI, lo montas usando el mount de Linux sin necesidad de ningún parámetro especial,
y metes en él los contenidos de hddr1016.zip)

Empezamos creando un fichero para contener la imágen de 200MB:
dd if=/dev/zero of=hdd200m.vhd count=200 bs=1M

Reiniciamos el HATARI con el DSK del driver (hddr1016.st), y con la imágen de disco que acabamos de crear.
Entramos en el diskette A: desde GEM, y ejecutamos HDDRUTIL.APP
Una vez que haya cargado, seleccionamos el DEVICE (ventana del medio), y luego nos vamos a las opciones de arriba a MEDIUM->FORMAT. Formateamos. Luego nos vamos a MEDIUM->PARTITION.
Particionamos, creando una sola partición que ocupe todo el disco. No hace falta que especifiques un tipo de partición,
ni que cambies ninguna otra opción.
Luego nos pide que reiniciemos. Reiniciamos y volvemos a cargar HDDRUTIL.APP.
En la ventana de arriba de las tres, seleccionamos el disco C:, y nos vamos a las opciones de arriba a FILE->INSTALL HDD DRIVER. Lo instalamos.
Ahora nos vamos otra vez a las opciones de arriba con el disco C: seleccionado, a SETTINGS->DEVICES AND PARTITIONS, y desmarcamos todo menos el 0.0, así no nos buscará todo lo demás en el arranque.
Ahora volvemos a FILE->INSTALL HDD DRIVER, y lo volvemos a instalar, ya con los settings que acabamos de configurar. Reiniciamos y listo.

Y ahora ya salimos de HATARI y ya podemos montar la imágen de disco así, paśandole el offset ya que como de costumbre estas imágenes tienen cosas antes de las particiónes:
sudo mount -t vfat -o loop,offset=1024 hdd200m.vhd hdd

Ahí ya metemos lo que queramos.
Tienes juegos instalados en disco a saco aquí:
https://atari.8bitchip.info/fromhd3.php
Cuando un juego no vaya bien, búscalo aquí:
http://d-bug.mooo.com/klaz/

-PARA MAYOR COMPATIBILIDAD, CONFIGURA EL SISTEMA COMO UN ***STE CON 4MB Y TOS 1.62 (LA MEJOR ROM PARA STE)***
 EL CORE REPLICA EXACTAMENTE UN STE DESDE LOS ESQUEMAS ORIGINALES DE ATARI: EN MODO ST HAY BUGS AÚN.

-PODRÍAS CONFIGURAR EL SISTEMA COMO UN ***MEGASTE DE 4MB Y TOS 2.06***, PERO EL CORE LO QUE REPLICA EXACTAMENTE
ES UN STE, NO UN MEGASTE.

-CONFIGURACIÓN RECOMENDADA EN EL FORO OFICIAL DE MISTER:
ST files config:
Memory: 1MB
TOS: TOS v1.02 (1987)
Chipset:ST
Blitter:Off
Viking: Off
Screen: Color
Border: Visible

STE files config:
Memory: 4MB
TOS: TOS v1.62 (1990)
Chipset:STE
Blitter:On
Viking: Off
Screen: Color
Border: Visibile

-Para descomprimir archivos .ARC en Linux, usa nomarch, que se puede instalar con apt-get.
-Para que un ejecutable se autoejecute en el inicio, simplemente mételo en AUTO. Tiene que tener extensión .PRG,
los .TOS sólo hay que renombrarlos y ya.

-Si tienes que reinstalar el AMBERSTAR, sólo se instala desde una TOS 1.x. Desde una 2.x, al pedirnos el disco B no lo reconoce y no permite seguir la instalación.

CÓMO INSTALÉ EL COMMANDER BLOOD EN DISCO DURO A PARTIR DE UNA IMÁGEN DE DISCO .ST
=================================================================================

Hay una versión ya instalada en https://atari.8bitchip.info/fromhd3.php pero se salta la música del principio.

Así que hacemos una pseudo-instalación desde una imágen de floppy en .ST

La versión que uso es "Captain Blood (1988)(ERE)(M3)[cr Elite]", la tienes en la colección TOSEC (En Myrient por ejemplo).
Cópialo a BLOOD.DSK, ábrelo desde el core de ST como un floppy normal (este .ST no se puede montar desde Linux, que sería lo más normal),
y en su raíz crea la carpeta AUTO y copia allí el BLOOD.PRG, para que luego se autoarranque cuando carguemos la imágen de disco.

Ahora, bajamos el programa IMGRUN de aquí: https://atari.8bitchip.info/imgrun.php
Para instalarlo, monta la imágen de disco duro, copia el directorio con todas las versiones y su TXT,
entra en el core, y allí selecciona el ejecutable IMGRF162.PRG (o el que corresponda a la versión de la TOS que estés usando)
haciendo un click con el botón derecho del ratón.
Y teniéndolo seleccionado, en el TOS ve a OPCIONES->INSTALAR APLICACIÓN, en TIPO DE DOCUMENTO por ST,
deja las demas opciones como están y dale al botón INSTALAR, y ahora dale a OPCIONES->GRABAR DESKTOP para que quede instalada de un inicio a otro.

Ahora mete el BLOOD.ST donde quieras (yo lo tengo en JUEGOS/BLOOD/BLOOD.ST) y al darle doble click ya lo lanza con el IMGRUN
y se ejecuta automáticamente lo que metiste en AUTO. 

NOTA PARA OTROS JUEGOS: Si el juego necesitase una TOS 1.04, tendrás que tener un fichero llamado T14UIDUB.FIC en el mismo fichero que el .ST
y de ese modo el IMGRUN carga una TOS 1.04 en RAM y ejecutaría el juego con más compatibilidad.
Pero para el Commander Blood en STE no hace falta, al parecer.

CONVERTIR IMÁGENES DE DISCO MSA AL FORMATO .ST QUE NECESITA EL CORE DE ATARI ST
===============================================================================

-Instalamos el Hatari, que viene con la herramienta hmsa que necesitamos:
sudo apt-get install hatari
-Nos vamos al directorio donde tenemos nuestros .msa, y hacemos:
for FILENAME in *.msa; do hmsa $FILENAME; done

IP ESTÁTICA EN LA MISTER (AL FIN!!!!)
=====================================

Créate un script en /Scripts, yo lo he llamado static_ip.sh, con este contenido y permisos de ejecución:

ifconfig eth0 up
ip addr add 192.168.1.39/24 dev eth0
ip route add default via 192.168.1.7
echo "nameserver 8.8.8.8" > /etc/resolv.conf

...Donde 192.168.1.7 es la IP del ordenador desde el que sales.
Tienes que tener una conexión cableada con IP estática en esa IP en el ordenador,
y tener el ordenador preparado para compartir la conexión WIFI, como haces para la Raspberry Pi (Ver en LINUX_KNOWLEDGE.TXT)
Tienes que ejecutar el comando para tener la IP estática.

CONECTAR POR WIFI USANDO UN PINCHO USB WIFI SOPORTADO POR EL KERNEL
===================================================================

¡¡CUIDADO!! Como el pincho que yo tengo no está soportado en el kernel, si se actualiza el kernel el pincho deja de funcionar
porque se nos borran el firmware blob y el módulo del kernel, y toda reinstalarlos siguiendo las instrucciones que voy a dar ahora.

LO PRIMERO, mi pincho WIFI está basado en el chipset RTL8188eu, que no viene soportado en el kernel.
Así que hay que proporcionar el módulo del kernel.

PUEDE QUE TENGAMOS QUE COMPILAR EL MÓDULO DEL KERNEL O PUEDE QUE NO.

CASO 1: NO HACE FALTA COMPILARLO
NORMALMENTE, SI NO CAMBIA LA VERSIÓN DEL KERNEL, TIENES EL MÓDULO YA PREPARADO Y SÓLO ES HACER:
cp rtl8188eu.ko /lib/modules/5.13-MiSTer/

CASO 2: NOS TOCA COMPILARLO PORQUE HA CAMBIADO LA VERSIÓN DEL KERNEL
Se hace así:
-Instalamos dependencias previas: apt-get install gcc-arm-linux-gnueabihf build-essential
-Clona el directorio del driver wifi: git clone --depth 1 https://github.com/ulli-kroll/rtl8188eu.git
-Clona el directorio de los sources del kernel de la MiSTer: git clone --depth 1 https://github.com/MiSTer-devel/Linux-Kernel_MiSTer.git
-De ambos directorios, borra su .git
-Entra en el directorio de los sources del kernel y haz:
make ARCH=arm mrproper && make ARCH=arm MiSTer_defconfig && make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- EXTRAVERSION=-MiSTer modules_prepare
-Ahora entra en el directorio de los sources del módulo wifi y haz:
make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- clean && make ARCH=arm CROSS_COMPILE=arm-linux-gnueabihf- KSRC=/home/manuel/src/mister/Linux-Kernel_MiSTer modules

FIN CASOS POSIBLES, LO QUE VIENE AHORA YA ES SIEMPRE IGUAL.

Ahora lo subimos a la MiSTer y lo copiamos a su sitio:

cp rtl8188eu.ko /lib/modules/5.13-MiSTer/

Y ejecutamos esto:

depmod -a
modprobe rtl8188eu

En este punto, podemos hacer iwconfig para ver que efectivamente el interface wifi nos aparece levantado.

Ahora vamos a configurar la conexión WIFI en sí.

Edita /media/fat/linux/wpa_supplicant.conf y pon ahí tu config wifi en el formato correcto, que es:
ctrl_interface=/run/wpa_supplicant
update_config=1
country=ES

network={
        ssid="tu_ssid_aqui"
        psk="tu_clave_aqui"
}

Ahora es cosa de reiniciar, ejecutar wifi.sh y listo.

***********Cambiar la fuente de la consola TTY local de Busybox/Linux*******************************************************************
NOTA PREVIA: Recuerda que estos ajustes no sobrevivirán a una actualización del sistema Linux de la MiSTer
porque en esas actualizaciones se sobreescribe /linux/linux.img que es donde vive el /etc del sistema embedido de la MiSTer!
NOTA PREVIA: Si liamos cualquier movida y el sistema no arranca por haber modificado alto en /etc,
tenemos que sacar la SD y montar el archivo /linux/linux.img, que es la partición EXT4 que contiene el sistema embedido de la MiSTer.
No hace falta ningún parámetro ni nada, un "mount" mondo y lirondo funciona.
NOTA PREVIA: Lo hacemos así porque si hacemos el setfont en /etc/profile o en un script de /etc/profile.d/,
no surte efecto hasta después de loguearnos, lo cual es feo y poco práctico.

-Nos traemos la fuente que queramos de un sistema GNU/Linux completo (yo uso la TerminusBold de 24x12):
scp /usr/share/consolefonts/Lat2-TerminusBold24x12.psf.gz root@192.168.207.xxx:/usr/share/consolefonts/

-Editamos /etc/inittab, buscamos "setfont" y dejamos la línea así:
::sysinit:/bin/setfont /usr/share/consolefonts/Lat2-Terminus24x12.psf.gz

-Reiniciamos y listo!

Si queremos autologin, ahí mismo en /etc/inittab, buscamos "agetty" y dejamos la segunda línea de "agetty" así:
console::respawn:/sbin/agetty --nohostname -L -a root tty1 linux

***********Montar el VHD de MSDOS (AO486) para la MiSTer********************************************************************************

mount -o loop,offset=32256 msdos622.vhd /mnt

***********MONTAR LA IMÁGEN DE DISCO DEL CORE ZXNEXT PARA AÑADIRLE O QUITARLE COSAS*****************************************************

sudo mount -o loop,rw,offset=65536 boot.vhd /mnt

NOTAS CORE MSX
==============

Tienes TODO el software de MSX1/MSX2 en: https://download.file-hunter.com/

Nos bajamos el último core de:
https://github.com/MiSTer-devel/MSX1_MiSTer/tree/MSX2test/releases

Ahora descargamos descargamos el CreateMSXPack de:
https://github.com/MiSTer-devel/MSX1_MiSTer/tree/MSX2test/releases
Y lo descomprimimos.

Ahora descargamos el romset completo de las BIOS de MSX1 y MSX2 de:
https://download.file-hunter.com/System%20ROMs/
(El archivo que se llama "Full Set System Roms")
...Y metemos su contenido tal cual (es decir, lo directorios "extensions", "machines", "misc"...)
en el directorio ROM del CreateMSXPack.
Descargamos también el archivo "Konami's Game Master - Konami (1985) [Japanese version] [RC-735] [GoodMSX] [1271].zip "
de:
https://download.file-hunter.com/Games/MSX1/ROM/
..Y tras descomprimirlo, lo copiamos como: ROM/extensions/gm2.rom
Entramos en ROM/extensions/ y descomprimimos mfrsd.gz con: gunzip mfrsd.gz

Ahora por fin ejecutamos el createMSXpack con:
python3 createMSXpack.py
...y no nos debería decir que falte nada.

Ahora copiamos el directorio MSX con todo lo que tiene dentro a la SD de la MiSTer. Lo metemos en /media/fat/games/MSX1.

Ahora, en el core, en "Load ROM PACK" cargamos el .MSX del modelo que queramos
(un buen modelo MSX2 PAL muy popular en Europa es el Philips VG 2835).
Y en "Load FW PACK" cargamos el .MSX del pack de firmware que queramos (tenemos dos: CART_FW y CART_FW_EN). Ahí es donde va
el firmware del FM PACK y todo es.
En "SLOT A:" ponemos "FM-PACK" o "SCC" o lo que queramos, y en "SLOT B:" cargamos el juego que queramos, eligiendo el mapper
correcto para cada juego.

PARA SNATCHER EN INGLÉS DESDE VHD, NECESITAMOS ARRANCAR CON EL "MegaFlashROM SCC+ SD" EN EL "SLOT A",
Y MIENTRAS ARRANCA, PRESIONAMOS "S" PARA ACTIVAR EL MODO DE COMPATIBILIDAD CON EL SCC ESPECIAL "SCC-I" QUE USA EL SNATCHER Y SD-SNATCHER.
Más trucos para juegos desde disco duro:
https://www.msx.org/wiki/MegaFlashROM_SCC%2B_SD_compatibility_settings 

PARA SNATCHER EN INGLÉS DESDE FLOPPY, NECESITAMOS TENER 256 KB de RAM, así que editamos el XML de la máquina que queramos, buscamos "RAM MAPPER"
y cambiamos el "block_count" de 4 a 16, y volvemos a ejecutar el python3 createMSXpack.py

***********Montar un VHD de MSX para la MiSTer******************************************************************************************

---Averiguamos dónde empieza con parted. Necesitamos saberlo en BYTES para poder pasárselo a mount, NO en sectores como nos lo dice FDISK. Por eso usamos parted:

sudo parted msx.vhd
(parted) unit                                                             
Unit?  [compact]? B                                                       
(parted) p                                                                

Number  Start   	End         	      Size                     Type        File system  Flags
 1      	   65536B    131137535B  131072000B  primary  fat16

---Nos quedamos con el 65536. Y montamos así:

sudo mount -t vfat -o loop,offset=65536 boot.vhd /mnt

FORMA ALTERNATIVA USANDO KPARTX

sudo kpartx -a -v fichero.vhd
(salen por pantalla los devices):

sudo mount /dev/mapper/loop0p2 /mnt/

luego umount y luego kpartx -d fichero.vhd y eso borra los mapper devices

te hace un device por cada particion

*********ACTUALIZAR LA MISTER********************************************************

Usa el script update_all de aquí:
https://github.com/theypsilon/Update_All_MiSTer
Es bajarse un ZIP, descomprimirlo en Scripts/Updates, y luego ejecutarlo. Entra en el menú la primera vez que lo ejecutes
pulsando UP. Allí cambia en la primera opción que los cores que te baje sean los de DB9.

*********NOTAS CORE PC98**************************************************************

Necesitas la BIOS, que puede estar en dos sitios distintos:
En /Games/Zet98/ con el nombre boot.rom
En /bootrom/ con el nombre Zet98.rom
Pero es el mismo fichero. Lo tienes en este hilo:
https://misterfpga.org/viewtopic.php?f=36&t=167&start=75

Pon GDC CLOCK a 2.5, viene puesto a 5 y eso jode algunos juegos.

Los juegos deben estar en .D88, y los puedes convertir desde cualquier otro formato con el Virtual Floppy Image Converter 011010,
que se baja de aquí:
https://www.vector.co.jp/download/file/win95/util/fh212773.html

Tienes muchos juegos ya traducidos y de todo en:
http://www.pc98.org/main.html
...Pero están en .HDI y se necesita que estén en HDF. Así que no te valen por ahora.

********CONFIGURAR EL TECLADO EN EL CORE DE AMIGA PARA USAR UN TECLADO DE PC EN ESPAÑOL**********

Nos bajamos este archivo:
http://aminet.net/util/wb/Win95e_V2.lha 
... Y lo mandamos a fat/games/amiga/shared

Ahora cargamos el ADF llamado INSTALLRTG.ADF, y dentro de él ejecutas INSTALLSHARE, que montará el directorio fat/games/amiga/shared, haciéndolo accesible desde Amiga.

Una vez hecho esto, nos vamos al CLI (GoShell en AG+) y hacemos:

mister:
lha x win95e_v2.lha
copy win95e devs:keymaps

Y ahora nos vamos a Preferences/Input, y seleccionamos el mapa win95e. ¡Y ya está!

********INSTALAR LAS KICKSTART PARA WHDLOAD*********************************************

Tienes las ROMs ya preparadas en shared/Kickstarts

Cargas el ADF llamado INSTALLRTG.ADF, y dentro de él ejecutas INSTALLSHARE, que montará el directorio fat/games/amiga/shared, haciéndolo accesible desde Amiga.

Una vez hecho esto, nos vamos al CLI (GoShell en AG+) y hacemos:

makedir devs:kickstarts
mister:
cd kickstarts
copy ALL to devs:kickstarts

********Añadir juegos al AGS (Arcade Game Selector)**************************************

El AGS escanea todos los directorios que acaben en .ags, así que puedes crear el directorio "Juegos.ags" y dentro de él
los directorios "A.ags", "B.ags", "C.ags", etc...
Y en cada directorio de cada letra, cada juego tendrá su "Nombre del juego.run"
(OJO: con permiso de ejecución que le damos con "PROTECT fichero +SE"),
su icono en "Nombre del juego.iff" y su texto de información "Nombre del juego.txt"

Puedes bajarte los thumbnails de todos los juegos en formato compatible con OCS de:
https://github.com/henrikstengaard/eab-whdload-games-ocs-menu-package.git

********ACTUALIZAR LA DISTRO AMIGA AGS+**************************************************

-Pones el LHA de la actualización en fat/games/amiga/shared
-Ahora arrancas el AGS+, cargas el ADF llamado INSTALLRTG.ADF, y dentro de él ejecutas INSTALLSHARE, que montará el directorio fat/games/amiga/shared, haciéndolo accesible desde Amiga.
RECUERDA que tienes que marcar lo de VENTANA->MOSTRAR->TODOS LOS FICHEROS para poder ver los archivos de la carpeta SHARED.
-Abres el LHA de actualización con el mARK (en la barra de arriba), lo descomprimes a RAM:, y dentro hay un script de actualización.
Se ejecuta y ya.

*********APUNTES CORE DE PSX*************************************************************

-Para que cada juego tenga su memory card en forma de .SAV en /media/fat/saves/PSX, mete cada juego en una carpeta que se llame
exactamente igual que el .CHD del juego (sin la extensión .CHD, claro).
En el menú debes tener activas las opciones "Automount Memory Card 1" y "Autosave".
(O si no tienes Autosave, tendrías que darle manualmente a "Save Memory Cards" cada vez que quieras volcalas a la microSD).
NO necesitas ningún fichero .MCD ni nada por el estilo.

-Notas del image crop, por el creador del core:
if you want a crt-like experience: use vblank and hblank on and set hcrop and vcrop on to 224
if you want as less black borders as possible and maximum viewable area, the default settings
(hblank fixed, vblank unfixed, no crop) works for 95% of the games. For the other 5% you need to optimize

=================================
= APUNTES CORE AMIGA (MINIMIG)  =
=================================

CREAR IMÁGEN CON SISTEMA DE ARCHIVOS PFS3
=========================================

-Instalamos pip3 que es el gestor de paquetes de python:
sudo apt-get install python3-pip

-Instalamos las amitools:
sudo pip3 install amitools

-Nos bajamos el filesystem PFS3 libre, llamado PFS3aio, de aquí:
http://aminet.net/package/disk/misc/pfs3aio

-Creamos la imágen del disco RBD, la inicializamos reservando 4 cilindros de espacio RBD para el driver de filesystem PSF3,
le instalamos el driver de filesystem PFS3, etc:

rdbtool -f -p DH test.hdf create size=512Mi + init rdb_cyls=4 + fsadd pfs3aio dostype=PFS3 + add size=98% name=DH0 fs=PFS3 bootable=true automount=true pri=1 Bla max_transfer=0x0001fe00 mask=0x7ffffffe num_buffer=30

-Formateamos la partición desde AmigaOS, pinchando en el icono y dándole a icon->format. Al final nos saldrá una confirmación de que estamos
 usando PFS3AIO.

NOTA: Podemos crear una imágen de disco adicional secundaria para los juegos de Laserdisc así (en esta no ponemos una partición bootable
y tampoco necesitamos una prioridad por lo mismo, etc):

rdbtool -f -p DH laserdisc.hdf create size=1200Mi + init rdb_cyls=4 + fsadd pfs3aio dostype=PFS3 + add size=98% name=DH0 fs=PFS3 automount=true Bla max_transfer=0x0001fe00 mask=0x7ffffffe num_buffer=30

OJO!!! Para que el core Minimig de la MiSTer reconozca ambas imágenes a la vez, tenemos que ejecutar HDToolBox con ambas imágenes conectadas,
y nos dirá que ha habido cambios o algo de eso: le damos a salvar cambios, y al reiniciar ya nos reconoce las dos imágenes y nos las monta normal.

    ---HERRAMIENTAS ADICIONALES--

PARA VER LA INFO DE LAS PARTICIONES DE LA IMÁGEN (el dostype, el maxtransfer, si es bootable o no...) LO HACEMOS ASÍ:
rdbtool test.hdf info

SI QUEREMOS MODIFICAR PARÁMETROS DE UNA PARTICIÓN YA CREADA (hacerla bootable, su dostype, su maxtransfer, etc...) LO HACEMOS ASÍ:
rdbtool test.hdf change 0 dostype=DOS5 max_transfer=0x1fe00 bootable=true
(En el ejemplo se modifica la partición número 0, para ver el número de las particiones usa "rdbtool test.hdf info").
(Recuerda que KS 3.1 sólo soporta hasta DOS5 para FFS, para DOS7 ya necesitarías KS 3.1.4 en adelante. Para KS 1.3 sería DOS1 o DOS0).

CREAR IMÁGEN DE DISCO CON SISTEMA FFS PARA WB 1.3
=================================================

Creamos la imágen de disco del tamaño que queramos en la propia MiSTer, con el comando:

dd if=/dev/zero of=Amiga\ 1.3\ HDD.hdf bs=128M count=1

Para particionar y formatear el disco, tienes dos opciones:

-Para sistemas 1.3, arrancamos el disco "A590 HD Setup Disk" y  

-Para sistemas 2.x/3.x, arrancamos con un ADF del Workbench (disco "INSTALL" de un set completo de discos del WB)

En ambos casos, en HDToolBox vamos a "Change Drive Type" y pinchamos en "Read Configuration From Drive" y nos lo detectará
correctamente como un disco del tamaño que lo hayamos creado.
Aplicamos los cambios, le damos a "Partition Drive" para dejar sólo una partición, REINICIAMOS
(EL SISTEMA NO VERÁ EL DISCO HASTA QUE REINICIEMOS!!!) y formateamos.
Luego puedes instalar el Workbench en el disco:

-Para instalar WB1.3, tienes que arrancar desde el disco "A590 HD Setup Disk"
y usar la utilidad de formateo que trae, que también te instalará el sistema.  

-Para instalar WB 2.x o 3.x, usamos el HDInstall que trae el primer disco de WB.

Para montar el HDF, NUNCA LO MONTES DESDE LINUX (AUNQUE SEA POSIBLE HACERLO)

-En cualquier sitema, usa el FS-UAE para montar el HDF y un directorio compartido de Linux, que funciona desde cualquier versión de Kickstart.
Para instalar FS-UAE y FS-UAE-LAUNCHER, que es con lo que se hace todo: https://fs-uae.net/download#ubuntu
A parte, en sistemas 3.x podrías InstallRTG.adf (que procede de las utilidades de Amiga para la MiSTer) que trae dentro InstallShared,
que monta el directorio /media/fat/games como "Mister:" en AmigaOS.

Si necesitas el LHA
-Para ROM 1.3 necesitas esta versión: http://aminet.net/package/util/arc/LhA_e138
-Para ROMs 2.x y 3.x necesitas este: https://aminet.net/package/util/arc/lha

Para hacer un archivo ejecutable hazle: protect <archivo> +e
Y si es un script ejecutable: protect <archivo> +se 

Para ejecutar juegos instalados en disco en ROM 1.3, como WHDLoad no funciona porque requiere 2.x, puedes usar JST para
cargar slaves de WHDLOAD: https://jotd.pagesperso-orange.fr/amiga.html

Tienes siempre las últimas versiones de los installs ya preparados de WHDLoad en:
http://ftp.grandis.nu/turran/FTP/Retroplay%20WHDLoad%20Packs/Commodore_Amiga_-_WHDLoad_-_Games
Y tienes todos los juegos en ADF desprotegidos aquí (porque algunos juegos se instalan desde ADF):
http://ftp.grandis.nu/turran/FTP/TOSEC/Games/Commodore Amiga - Games - [ADF]/

Si usas WHDLoad, no te olvides de crear s:whdload.prefs con el contenido:
PRELOAD
SplashDelay=0
ConfigDelay=0

Para dar permisos de ejecución a un archivo es con: PROTECT nombre_archivo e
Si no puedes borrar un archivo porque está protegido: PROTECT nombre_archivo d
Puedes ver los permisos de los archivos con el comando LIST, y a partir de ahí las modificas como quieras.
Por ejemplo, para que tenga permiso de lectura, escritura, ejecución y borrado sería:
PROTECT nombre_archivo rwed

Para pasar de NTSC a PAL y viceversa, puedes usar NTSC-PAL de Nico Francois, que son dos ejecutables que vienen en
el FISH DISK 0387, que tienes aquí: http://aminet.net/package/misc/fish/fish-0387
Recuerda que necesitas un ECS al menos para que funcione.
JST permite también ejecutar en PAL/NTSC los juegos, con el tooltype PAL o NTSC (o sea, pasándole el parámetro PAL o NTSC a JST).

¿PAL o NTSC? El sistema, arráncalo en NTSC siempre (o sea, configura el chipset como NTSC).
Y luego, los juegos que tengan una banda gruesa abajo ponlos en NTSC y los que estando en PAL ocupen la pantalla completa,
ejecútalos en PAL.

CONSEJOS GENERALES PARA INSTALAR JUEGOS EN DISCO DURO EVITANDO USAR INSTALACIONES WHDLOAD:
-Mira en HOL (https://hol.abime.net) si el juego tiene instalador, o si hay notas sobre su instalación a mano.
 Si tiene instalador nativo o se puede instalar a mano, mira a ver si el juego tiene versiones crackeadas
 en http://ftp.grandis.nu/turran/FTP/TOSEC/Games/Commodore Amiga - Games - [ADF]
 y lo debes instalar de una versión crackeada de esas.
 (RECUERDA que puedes buscar el ejecutable del juego usando el comando FILE de GNU/Linux para ver qué archivos son ejecutables de AmigaOS).
 PARA SABER QUÉ ETIQUETA TIENE UN DISKETTE (POR SI TIENES QUE HACER ASSIGNS) SIMPLEMENTE EJECUTA EL COMANDO "ASSIGN" SIN PARÁMETROS
 PARA VER LOS ASSIGNS QUE HAY HECHOS AL TENER EL DISKETTE(ADF) METIDO.
-Si no, busca un instalador nativo casro en http://ftp.grandis.nu/turran/FTP/Retroplay%20WHDLoad%20Packs/Commodore_Amiga_-_HD_Loaders_-_Games/ 
-Si no, ya te toca tirar de JST para cargar los slaves de WHDLOAD.

PARA QUE VAYAN LOS JUEGOS DE WHDLOAD EN VERSIÓN NTSC:
Ve a Storage:Monitors y arrastra el monitor NTSC a Devs:Monitors, y pon el chipset ECS (el OCS-A500 no cambia
entre PAL y NTSC al vuelo).

PARA QUE SE VEAN LOS ICONOS DE WHLOAD:

OPCIÓN 1: INSTALAR NEWICONS
Nos lo bajamos de https://aminet.net/package/util/wb/NewIcons46, extraemos, copiamos C/Newicons a C:,
copiamos Libs:NewIcons.library a Libs:, y añadimos al final de S:Startup-Sequence:
Run <>NIL: C:NewIcons
Wait 1
....Y aquí ya cargaríamos WB, o TinyLauncher o lo que sea.

OPCIÓN 2: INSTALAR ICONLIB
Bájate esto:  https://aminet.net/package/util/libs/IconLib_46.4
De ahí, copia Libs/68000/icon.library a Libs:, y copia ThirdParty/RemLib a C:
Ahora edita S:Startup-Sequence, y al principio del todo, añade:

RemLib >NIL: icon.library

...Eso descargará la icon.library de la ROM y hará que se cargue Libs:icon.library.
Reinicia y ya debería verse los iconos de los juegos de WHDLoad sin problema.

UN STARTUP-SEQUENCE MÍNIMO PARA TINYLAUNCHER (ideal para sistemas con poca memoria)

SetPatch >NIL:
Stack 8192
Makedir RAM:ENV
Assign ENV: RAM:ENV
Run <>NIL: C:NewIcons
Wait 1
SYS:Programs/TL/TinyLauncher.exe
reboot

*********NOTAS CORE PDP2011**************************************************************

Los discos de Unix sólo funcionan en los modelos 11/45. Para BSD2.x, es con una 11/70.
Una vez que estás en el @, escribe unix y dale al intro. Puedes logearte como root, sin password.
Más instrucciones de uso aquí: https://pdp2011.sytse.net/wordpress/howto/running/

*********NOTAS CORE ENTERPRISE***********************************************************

Según arrancas, empieza en BASIC.
Puedes poner :HELP para ver los comandos.
Para entrar en el sistema operativo de disco, pon :EPDOS

*********Mando analógico en el core de SATURN usando el Retro-Bit Saturn Wireless Pad****

-Ponemos el mando en modo XInput. Para ello mantenemos pulsado START+B hasta que se queden encendidas dos luces en vez de una.
-En el menú principal de la MiSTer, configuramos el mando. Es MUY IMPORTANTE que cuando te pida los TILT del Joystick 1 y 2
los presiones correctamente.
-En el core de SATURN, en las opciones de INPUT ponemos "3D PAD".
-En el core de PSX, ponemos el mando 1 como DualShock.

*********Mando 8BitDo M30 2.4G en la MiSTer**********************************************

Para evitar que tarde 5 segundos en responder cuando enciendes la MiSTer o cuando entras en un core, ponlo en modo DInput.
Para ello, mantén pulsado HOME (botón con un corazón pixelado) + START durante 5 segundos. NO esperes ninguna luz,
simplemente cambiará de modo y ya no tardará en responder al iniciar la MiSTer o al cargar un core.

**********Notas core Apple II/e**********************************************************

Los juegos deberías bajarlos siempre de https://archive.org/details/apple-ii-play-it-by-year-collection-v-0.1
(En Myrient están separados por formato, son los mismos).

Los .wo, .woz, .wav... son incompatibles con el core. Para eliminarlos:
find . -type f -name '*.wav' -delete
find . -type f -name '*.wo' -delete
find . -type f -name '*.woz' -delete

ATENCIÓN: Procura buscar las imágenes de disco duro que vienen en la colección, y usar eso en vez de imágenes de diskette.
Para buscar todas las imágenes de disco duro que vienen, haz:
find ./ -name \*PRODOS\*
Luego, renombra esos ficheros a .HDV y trátalos como imágenes de disco duro.

Algunos .po son en realidad .do, y viceversa. Si un disco no te arranca, cambia su extensión y vuelve a probar.

Algunos juegos puedes encontrarlos en .2MG, que se convierten muy fácilmente a .HFV:
dd if="Ancient Legends - 800k version.2mg" of="Ancient Legends - 800k version.hdv" bs=64 skip=1

Algunos juegos puedes encontrarlos en .SHK,NO INTENTES CONVERTIRLOS, ES INFERNAL. OLVÍDATE DE ELLOS. 

Puedes ver el texto a 80 columnas correctamente poniendo AUDIO & VIDEO -> PIXEL CLOCK a DOUBLE.
El texto a 80 columnas es una característica del modeo Apple II/e, así que deja el PIXEL CLOCK a DOUBLE siempre.

**********Jugar al Prince of Persia de Apple II/e****************************************

El manual completo lo tienes en: http://www.abandonia.com/files/extras/26916_Extra%20document_Manual.pdf

Para cambiar entre teclado (J, K, L, I, U, O, ALT/WIN) y joystick, pulsamos CURSOR ARRIBA (para teclado) y CURSOR ABAJO (JOYSTICK).
Recuerda que con el teclado, para saltar mientras corres, tienes que usar U, O, no puedes usar I, que es la tecla de salto vertical.
Otros atajos son:
CRTL+S: activar y desactivar sonido.
CTRL+A: Reiniciar nivel.
CTRL+V: Mostrar la versión del juego.

*********Notas core Macplus**************************************************************

Para tener imágen con escalado perfecto en 1080p, añadimos a MiSTer.ini:

[macplus]
vscale_mode=2

O bien:

[macplus]
video_mode=1920,1026,60

...pero mejor lo de vscale.

*********COMPILAR CORES DE MISTER********************************************************

Se compilan con el Quartus Lite. 
Algunos cores al compilar te darán errores tipo:

Error (12006): Node instance "pll_hdmi" instantiates undefined entity "pll_hdmi". Ensure that required library paths are specified correctly, define the specified entity, or change the instantiation. If this entity represents Intel FPGA or third-party IP, generate the synthesis files for the IP.

En tal caso tienes que usar Quartus Lite 17.0.0, y compilarán correctamente. Es la versión que recomienda Sorgelig.
Una vez instalada, si ves que lanzándolo con el icono del escritorio no pasa nada, ejecútalo a mano así para ver cuál es el problema:

cd ~/intelFPGA_lite/17.0/quartus/bin
quartus --64bit

Probablemente sea que necesita libpng12 en lugar de libpng16 que es lo que usa Debian Testing a día de hoy,
así que descarga el paquete de libpng12 de aquí:
http://snapshot.debian.org/archive/debian/20160413T160058Z/pool/main/libp/libpng/libpng12-0_1.2.54-6_amd64.deb
E instálalo con "sudo dpkg -i" y ya debería funcionar en un Debian reciente.

*********HACER QUE EL UPDATE_ALL NO ACTUALIZE LOS CHEATS*********************************

Editamos /media/fat/downloader.ini y añadimos:

[mister]
filter = !cheats

Hay otros filtros. La lista está aquí: 
https://github.com/MiSTer-devel/Distribution_MiSTer#list-of-tags-that-you-may-use-with-the-downloader-and-the-custom-download-filters-feature


COMO LO VENIMOS HACIENDO AHORA MISMO:

[mister]
filter = !cheats !acornatom !acornelectron !adam !adventurevision !alicemc10 !altair8800 !amstrad-pcw !apogee !apple-i !aquarius !arduboy !atari2600 !atari7800 !atarilynx !ay-3-8500 !bbcmicro !bk0011m !c16 !channelf !arcadia !archie !astrocade !bbcbridgecompanion !c16 !casio_pv-1000 !casio_pv-2000 !chess !chip8 !coco2 !coco3 !coleco !colecoadam !colecovision !creativision !edsac !eg2000 !enterprise !epochgalaxyii !flappybird !galaksija !gamate !gamenwatch !genesis-core !gnw !gameoflife !handheld2p !homelab !intellivision !interact !jupiter !laser !laser310 !lynx48 !megacd !megaduck !msx !multicomp !myvision !n64 !odyssey2 !ondra_spo186 !orao !oric !other-cores !pc88 !pc8801 !pdp1 !pet2001 !pmd85 !pocketchallengev2 !pokemonmini !ql !rx78 !s32x !samcoupe !scv !segacd !sgb !sharpmz !slugcross !sms !sordm5 !specialist !supervision !supervision8000 !super_vision_8000 !svi328 !tatungeinstein !ti994a !tk2000 !tomyscramble !tomytutor !trs-80 !tsconf !uk101 !vc4000 !vector-06c !vectrex !vic20 !vt52 !wonderswan !wonderswancolor !x68000 !zx48 !zx81 !zxnext

Y para que no nos instale el core de NeoGeo Pocket, añadimos en "[jtcores]" el filtro "!console":

[jtcores]
db_url = https://raw.githubusercontent.com/jotego/jtcores_mister/main/jtbindb.json.zip
filter = [MiSTer] !console

**********APUNTES CABLE VGA->SCART********************************************************

--Debes poner en el MiSTer.ini: 
"direct_video=1"
"composite_sync=1"
"vga_scaler=0" 

**********APUNTES DIRECT VIDEO************************************************************

--Debes poner en el MiSTer.ini:
"direct_video=1"
"composite_sync=1"
--El conversor que yo tengo, y que funciona más o menos, lleva un chip EP94Z1E.

**********MODO DE VÍDEO ESPECÍFICO PARA EL CORE MACPLUS***********************************

Añadimos al final de MiSTer.ini:

[MacPlus]
video_mode=1536,1026,60

**********NOTAS CORE PHILIPS CD-I (CDI)*********************************************************

Para convertir de CUE/BIN a CHDMAN, se hace como siempre con:
for i in *.cue; do chdman createcd -i "$i" -o "${i%.*}.chd"; done

...PERO si te da error, mira que en el .CUE no tengas "CDI/2352", porque esa es la causa.
Simplemente cámbialo por "MODE2/2352".

**********SOLUCIÓN A SINCRONÍA PERDIDA EN CIERTOS ARCADES*************

El problema es con algunos cores arcade de Jotego, como SplatterHouse, Vulcan Venture y otros.
Se solucionó en este hilo:
https://misterfpga.org/viewtopic.php?p=97633#p97633

Simplemente, añadimos estas líneas al final del MiSTer.ini:

[arcade]
video_mode=1920,1080,60
vsync_adjust=2

O podríamos hacerlo por separado, pero es más rollo:

[jtshouse]
video_mode=1920,1080,60
vsync_adjust=2

[jttwin16]
video_mode=1920,1080,60
vsync_adjust=2

Lo que hace es que los modos de vídeo precisos se calculan tomando como base el modo 1080p a 60Hz.
Esto es necesario por un error de cálculo al usar vsync_adjust=2 con estos cores: se calcula mal sin especificar este modo base.

***********CREAR DISCOS DE VIDEOCD PARA EL CORE CD-I***********************
INFO SACADA DE: https://github.com/Slamy/MPEG1_Handbook

PREQUISITOS:
--Nos bajamos el TMPGEnc-2.525.64.184 de Windows (es la última versión gratis, y nos sirve para MPEG-1).
  Está para bajar en su página oficial: https://www.tmpgenc.net/en/download.html
  Y si no está allí, en Archive.org 
--Instalamos esto en WINE, ya que al parecer TMPGENC lo necesita: winetricks allcodecs
--Nos bajamos DOS4GW.EXE, PINK.EXE, PINK.DLL de https://github.com/TwBurn/cdi-sdk/tree/master/TOOLS/Master
--Instalamos el K3B: apt-get install k3b
  Como el soporte de VCD/CD-I en K3B está mal en Debian, sustituimos unos archivos de la instalación:
wget http://www.icdia.co.uk/sw_app/vcd_on_cdi_411.zip
unzip vcd_on_cdi_411.zip
sudo cp CDI_IMAG.RTF /usr/share/k3b/cdi/cdi_imag.rtf
sudo cp CDI_TEXT.FNT /usr/share/k3b/cdi/cdi_text.fnt
sudo cp CDI_VCD.APP /usr/share/k3b/cdi/cdi_vcd.app
sudo cp icdia.htm /usr/share/k3b/cdi/icdia.htm
sudo cp vcd_on_cdi_41.pdf /usr/share/k3b/cdi/vcd_on_cdi_41.pdf

--AVERIGUAMOS EL FRAMERATE ORIGINAL DEL VÍDEO CON:
mpv "big_buck_bunny_1080p_h264.mov"
...Y miramos en la consola su framerate EXACTO.

 				BLOQUE DONDE SACAMOS EL VIDEO STREAM INICIAL (.AVI) Y EL VÍDEO STREAM INICIAL (.MP2)

VÍDEOS DE 25 FPS (Lo ideal y más sencillo):
--Sacamos el VÍDEO STREAM (.MP1)
ffmpeg -y -i "big_buck_bunny_1080p_h264.mov" \
    -filter_complex '[0:v]crop=ih/3*4:ih[cropped];[cropped]scale=352x288[scaled]' \
    -r 25 -map "[scaled]" -q:v 0 video.avi
--Sacamos el AUDIO STREAM (.MP2):
ffmpeg -y -i "big_buck_bunny_1080p_h264.mov" -b:a 224k -ar 44100 video.mp2

VÍDEOS DE 24 FPS (Hay que convertirlos a 25 FPS para que vayan suaves en PAL)
--Sacamos el VÍDEO STREAM (.MP1)
ffmpeg -y -i "big_buck_bunny_1080p_h264.mov" \
-filter_complex "[0:v]crop=ih/3*4:ih,scale=352x288,setpts=0.96*PTS[scaled]" \
-r 25 -map "[scaled]" -q:v 0 video.avi
--Sacamos el AUDIO STREAM (.MP2):
ffmpeg -y -i "big_buck_bunny_1080p_h264.mov" -filter:a "atempo=1.0416667" -ar 44100 -ac 2 -c:a mp2 -b:a 224k video.mp2

				FIN BLOQUE DONDE SACAMOS EL VIDEO STREAM INICIAL (.AVI) Y EL VÍDEO STREAM INICIAL (.MP2)

--RE-COMPRIMIMOS EL VIDEO STREAM DE .AVI A .MP1 (O .MPG):
En TMPGENC, le damos al botón "Browse" de "Video Source", y seleccionamos el .AVI.
En la pantalla principal:
---"Stream Type" a "ES [Video only]".
En Setting:
----PARA VÍDEOS PAL (25FPS):
"Aspect Ratio" a "4:3 625 Line (PAL)"
"Framerate" al valor que sacamos al principio con MPV.
"VBV buffer size" a 36.
----PARA VÍDEOS NTSC (30FPS):
"Aspect Ratio" a "4:3 525 Line (NTSC)"
"Framerate" al valor que sacamos al principio con MPV.
"VBV buffer size" a 38.

Volvemos al menú principal y le damos al botón "Start". Con esto obtenemos el .M1V.

--MULTIPLEXAMOS EL AUDIO Y EL VÍDEO:
En TMPGENC, vamos a File->MPEG Tools->Simple Multiplex, ponemos "Type" a "MPEG-1 Video-CD",
seleccionamos el stream de vídeo (.M1V) y el de audio (.MP2) y le damos a "Run".
Con esto obtenemos un .MPG que es compatible con VideoCD.

--Ahora, en K3B, nos vamos a "New Project->New VideoCD Project", arrastramos el MPG compatible, y le damos a "Burn".
  Se nos abrirá otro diálogo:
  En la pestaña "Writing" marcamos "Only create image". 
  En la pestaña "Settings" y nos aseguramos de que esté seleccionado "Video CD 2.0",
  y el soporte para CD-i.

--ALTERNATIVA: Se puede salvar el perfil .xml desde k3b, y como en realidad k3b simplemente usa vcdxbuild por debajo, simplemente hacer:
vcdxbuild --cue-file=videocd.cue --bin-file=videocd.bin example/vcd.xml

--El CUE+BIN quedará en ~/Videos, y lo puedes pasar a .CHD con el típico:
  for i in *.cue; do chdman createcd -i "$i" -o "${i%.*}.chd" -c cdzs,cdfl -hs 9792; done

			(¡¡¡INCOMPLTETO!!!) MÉTODO ALTERNATIVO: CREAR VÍDEOS EN FORMATO ESPECÍFICO DEL CD-I

--Extraemos el .MV1 (¿Directamente? Esto es raro...) y el .MP2:

ffmpeg -y -i "big_buck_bunny_1080p_h264.mov" -r 25 \
    -vf "scale=384:256" -b:v 1150k -minrate 1150k -maxrate 1150k -bufsize 224k bunny.m1v
    
ffmpeg -y -i "big_buck_bunny_1080p_h264.mov" -b:a 224k -ar 44100 bunny.mp2
  
--PARA MULTIPLEXAR AUDIO Y VÍDEO:
En el mismo directorio que el .MP2 y el .M1V, creamos un fichero que llamaremos "video.psc" con el contenido:
CDI "VIDEO01.MMD" pattern 0xFFFF from
    audio    "audio.mp2"
    start    0
    stream   0

    video    "video.m1v"
    start    0
    stream   0

--Ahora en DOSBOX hacemos:
PINK.EXE video.psc
...Con esto obtenemos el .MMD

...TODO: Falta saber cómo montar un "Green Disc" donde dentro vaya el .MMD metido.
